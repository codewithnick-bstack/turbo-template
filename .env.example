# Root .env.example — not used directly; each app has its own .env.example.
# See:
#   apps/api/.env.example    → apps/api/.env
#   apps/web/.env.example    → apps/web/.env.local
#   apps/admin/.env.example  → apps/admin/.env.local
#
# Run `make setup` to copy all example files automatically.
