{
  "name": "@repo/billing",
  "private": true,
  "version": "0.1.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'billing ready'",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/observability": "workspace:*",
    "stripe": "^17.4.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
